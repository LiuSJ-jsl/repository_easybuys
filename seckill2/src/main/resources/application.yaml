spring:
  application:
    name: seckill2
  datasource:
    url: jdbc:mysql://localhost:3306/seckill?useUnicode=true&characterEncoding=UTF-8&serverTimezone=CST
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

  #redis
  redis:
    host: 127.0.0.1
    password:
    port: 6379
    jedis:
      pool:
        max-active: 9
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 10000ms